# This rule is not used by the project data-platform-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the dataplatform.opsarray.io.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: data-platform-operator
    app.kubernetes.io/managed-by: kustomize
  name: dataplatform-editor-role
rules:
- apiGroups:
  - dataplatform.opsarray.io
  resources:
  - dataplatforms
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - dataplatform.opsarray.io
  resources:
  - dataplatforms/status
  verbs:
  - get
