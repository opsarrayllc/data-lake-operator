# Ingresses for the local DataPlatform on kind (nginx + mkcert wildcard).
# Applied by `make kind-up`. Backends exist after `make run` and applying
# config/samples/dataplatform_v1alpha1_local.yaml.
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak
  namespace: keycloak
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-buffer-size: 16k
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - keycloak.data-lake.local
    secretName: data-lake-tls
  rules:
  - host: keycloak.data-lake.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: keycloak
            port:
              number: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lakekeeper
  namespace: lakekeeper
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-buffer-size: 16k
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - lakekeeper.data-lake.local
    secretName: data-lake-tls
  rules:
  - host: lakekeeper.data-lake.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lakekeeper
            port:
              number: 8181
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trino
  namespace: trino
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-buffer-size: 16k
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - trino.data-lake.local
    secretName: data-lake-tls
  rules:
  - host: trino.data-lake.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: trino
            port:
              number: 8080
