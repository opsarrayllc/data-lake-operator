apiVersion: datalake.opsarray.io/v1alpha1
kind: DataLake
metadata:
  labels:
    app.kubernetes.io/name: data-lake-operator
    app.kubernetes.io/managed-by: kustomize
  name: datalake-sample
spec:
  # TODO(user): Add fields here
