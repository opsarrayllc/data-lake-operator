apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-lake-operator.fullname" . }}
  labels:
    {{- include "data-lake-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-lake-operator.selectorLabels" . | nindent 4 }}
