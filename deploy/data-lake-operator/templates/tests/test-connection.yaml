apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-lake-operator.fullname" . }}-test-connection"
  labels:
    {{- include "data-lake-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-lake-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
